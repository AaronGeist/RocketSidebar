<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/sidebar_width"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/sidebar_width" />

    <TextView
        android:id="@+id/sidebar_width_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/sidebar_width"
        android:layout_alignBottom="@+id/sidebar_width"
        android:layout_centerHorizontal="true" />

    <SeekBar
        android:id="@+id/sidebar_width_selector"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/sidebar_width" />

    <Button
        android:id="@+id/startBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/sidebar_width_selector"
        android:layout_below="@+id/sidebar_width_selector"
        android:layout_marginTop="14dp"
        android:text="Start" />

    <Button
        android:id="@+id/stopBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/startBtn"
        android:layout_alignBottom="@+id/startBtn"
        android:layout_toLeftOf="@+id/sidebar_width_text"
        android:text="Stop" />

</RelativeLayout>